export interface GalleryItem {
  id: string;
  url: string;
  title: string;
  description: string;
  date?: string;
}

export interface Soundtrack {
  url: string;
  title: string;
  artist: string;
}
